#[derive(Debug, Clone, PartialEq)]
pub struct Spacings {
    pub xxs: String,
    pub xs: String,
    pub s: String,
    pub m: String,
    pub l: String,
    pub xl: String,
    pub container_max_width: String,
}